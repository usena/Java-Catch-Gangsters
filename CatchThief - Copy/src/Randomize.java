import java.util.ArrayList;

// Interface for randomizing elements
public interface Randomize {

    // Method to randomize a list of Gangster objects
    ArrayList randomize(ArrayList<Gangster> gangsters);

    // Method signature for potential future use
    void randomize();
}
